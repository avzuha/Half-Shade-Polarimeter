# 🔬 Half-shade Polarimeter Simulation

A Python program to simulate the **half-shade polarimeter method** used in optics experiments.  
The simulation shows how the half-shade field changes with angle, using interactive visualization.

## 📌 Features
- Interactive angle slider (0°–360°).
- Visual representation of left and right half-shades.
- Red line indicates analyzer orientation.
- Uses `matplotlib` for plotting and `ipywidgets` for interactivity.

## 🛠 Requirements
Install dependencies:
```bash
pip install -r requirements.txt
